// Generated by CoffeeScript 1.6.3
var Loading,
  __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

Loading = (function() {
  function Loading(arg) {
    this.animate = __bind(this.animate, this);
    var cnt, height, id, speed1, speed2, width;
    cnt = arg || 0;
    id = cnt.id ? cnt.id : 0;
    width = cnt.width ? cnt.width : 0;
    height = cnt.height ? cnt.height : 0;
    speed1 = cnt.speed1 ? cnt.speed1 : 0;
    speed2 = cnt.speed2 ? cnt.speed2 : 0;
    this.canvas = document.getElementById(id);
    this.canvas.width = width;
    this.canvas.height = height;
    this.ctx = this.canvas.getContext("2d");
    this.width = width;
    this.height = height;
    this.angle1 = Math.random() * 360;
    this.angle2 = Math.random() * 360;
    this.speed1 = speed1;
    this.speed2 = speed2;
  }

  Loading.prototype.animate = function() {
    var ctx, h, rh, rw, w, x, y;
    window.requestAnimationFrame(this.animate);
    ctx = this.ctx;
    w = this.width;
    h = this.height;
    ctx.clearRect(0, 0, w, h);
    ctx.fillStyle = '#000';
    ctx.strokeStyle = '#000';
    ctx.ellipse(w / 2, h / 2, 50, 50);
    ctx.fill();
    ctx.ellipse(w / 2, h / 2, 85, 85);
    ctx.stroke();
    ctx.ellipse(w / 2, h / 2, 120, 120);
    ctx.stroke();
    this.angle1 += this.speed1;
    rw = 42.5;
    rh = 42.5;
    x = rw * Math.cos(this.angle1 / 180 * Math.PI);
    y = rh * Math.sin(this.angle1 / 180 * Math.PI);
    ctx.translate(w / 2, h / 2);
    ctx.fillStyle = '#000';
    ctx.beginPath();
    ctx.ellipse(x, y, 10, 10);
    ctx.fill();
    ctx.translate(-w / 2, -h / 2);
    this.angle2 += this.speed2;
    rw = 60;
    rh = 60;
    x = rw * Math.cos(this.angle2 / 180 * Math.PI);
    y = rh * Math.sin(this.angle2 / 180 * Math.PI);
    ctx.translate(w / 2, h / 2);
    ctx.fillStyle = '#000';
    ctx.beginPath();
    ctx.ellipse(x, y, 20, 20);
    ctx.fill();
    return ctx.translate(-w / 2, -h / 2);
  };

  return Loading;

})();

/*
//@ sourceMappingURL=loading.map
*/
