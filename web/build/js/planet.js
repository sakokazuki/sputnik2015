// Generated by CoffeeScript 1.6.3
var Planet,
  __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

Planet = (function() {
  function Planet(arg) {
    this.animate = __bind(this.animate, this);
    var cnt, height, id, speed, width;
    cnt = arg || 0;
    id = cnt.id ? cnt.id : 0;
    width = cnt.width ? cnt.width : 0;
    height = cnt.height ? cnt.height : 0;
    speed = cnt.speed ? cnt.speed : 0;
    this.canvas = document.getElementById(id);
    this.canvas.width = width;
    this.canvas.height = height;
    this.ctx = this.canvas.getContext("2d");
    this.w = width;
    this.h = height;
    this.angle = 0.0;
    this.speed = speed;
  }

  Planet.prototype.animate = function() {
    var ctx, rh, rw, x, y;
    window.requestAnimationFrame(this.animate);
    ctx = this.ctx;
    ctx.clearRect(0, 0, this.w, this.h);
    ctx.fillStyle = '#000';
    ctx.ellipse(this.w / 2, this.h / 2, 52, 52);
    ctx.fill();
    ctx.translate(this.w / 2, this.h / 2);
    ctx.rotate(-Math.PI / 6);
    ctx.strokeStyle = '#000';
    ctx.beginPath();
    ctx.ellipse(0, 0, 124, 18);
    ctx.stroke();
    ctx.rotate(Math.PI / 6);
    ctx.translate(-this.w / 2, -this.h / 2);
    this.angle += this.speed;
    rw = 62.0;
    rh = 9.0;
    x = rw * Math.cos(this.angle / 180 * Math.PI);
    y = rh * Math.sin(this.angle / 180 * Math.PI);
    ctx.translate(this.w / 2, this.h / 2);
    ctx.rotate(-Math.PI / 6);
    ctx.fillStyle = '#000';
    ctx.beginPath();
    ctx.ellipse(x, y, 20, 20);
    ctx.fill();
    ctx.rotate(Math.PI / 6);
    return ctx.translate(-this.w / 2, -this.h / 2);
  };

  return Planet;

})();

/*
//@ sourceMappingURL=planet.map
*/
